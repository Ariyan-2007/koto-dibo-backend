using Mapster;

namespace KotoDibo.Application.Mappings;

public class MappingConfig : IRegister
{
    public void Register(TypeAdapterConfig config)
    {
        // Entity <-> DTO mappings are registered here as features are implemented.
    }
}
